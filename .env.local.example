# No environment variables are required yet — this file is scaffolding for
# the next developer. Copy to `.env.local` and fill in values as real
# integrations (analytics, forms, CMS, etc.) are added.

# NEXT_PUBLIC_SITE_URL=https://example.com
